import PageContainer from 'containers/PageContainer';

const Mypage = () => {
  return (
    <PageContainer header footer={false} margin_bottom={false}></PageContainer>
  );
};

export default Mypage;
